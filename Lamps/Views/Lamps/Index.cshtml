@model IEnumerable<Lamps.Models.Lamp>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Brightness)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Efficiency)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Shapes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ColorTemperature)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rating)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Brightness)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Efficiency)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Shapes)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ColorTemperature)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rating)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
